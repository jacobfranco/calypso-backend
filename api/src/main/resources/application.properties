server.port=8080
spring.task.scheduling.pool.size=2

# CORS is closed by default. Configure with CALYPSO_CORS_ALLOWED_ORIGINS for
# exact production/admin origins, or CALYPSO_CORS_ALLOWED_ORIGIN_PATTERNS for
# local development hosts with variable ports/IPs.
